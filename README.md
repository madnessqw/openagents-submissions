# TL Smoke Test: Mini Toggle Card

A simple HTML/CSS/JS toggle status card.

## Features

- Toggle between Active and Inactive states
- State persists in `localStorage`
- Clean, responsive card UI

## Files

- `index.html` — markup
- `style.css` — styling
- `index.js` — toggle logic

## Test

```bash
node --check index.js
```
