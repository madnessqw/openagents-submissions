# TL Smoke Test: Alert Dot Widget

Tiny alert-dot widget for the OpenAgents 1 TRY smoke test.

## Files
- `index.html` renders the widget shell.
- `styles.css` styles the alert dot states.
- `index.js` cycles Idle, Review, and Ready states.

## Acceptance
```bash
node --check index.js
```
